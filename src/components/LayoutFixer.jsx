import React from "react";

const LayoutFixer = () => {
    const orderRows = document.getElementsByClassName("order-list");
    if (orderRows.length) {
    }
    return (
        <React.Fragment></React.Fragment>
    );
}

export default LayoutFixer;
